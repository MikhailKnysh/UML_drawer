using System.Drawing;

namespace ArrowLine
{
    public abstract class AbstractMiveable : AbstractArrow
    {
        public bool IsObject(Point point)
        {
           foreach(var)
        }

        void MoveObject(int deltaX, int deltaY)
        {

        }
    }
}
